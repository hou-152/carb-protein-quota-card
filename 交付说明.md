# 碳水蛋白质配额卡 · 交付说明（给侯先生）

> 维护时间：2026-06-01　维护人：Claude Code
> 一句话：把"减脂增肌饮食方案计算器"的最新版做了结构优化并部署上线，计算逻辑一个字没改。

---

## 一、这次到底做了什么（人话版）

1. **没有动它的算法**。性别/身高/体重/有氧/分餐怎么算碳水蛋白质，全部保持原样。
   - 证据：打包出来的程序文件指纹（哈希）维护前后完全一样。
2. **只优化了"结构"**，让它能稳定上线、能塞进你的个人主页：
   - 资源路径改成相对路径 → 放到任何网址下都不会白屏。
   - 补了网站图标、分享预览信息 → 别人转发链接时显示正常。
3. **把最新版部署上线了**（见下面"线上地址"）。

---

## 二、线上地址（现在就能打开）

| 通道 | 地址 | 状态 | 说明 |
|---|---|---|---|
| GitHub Pages | https://hou-152.github.io/carb-protein-quota-card/ | ✅ 已上线最新版 | 我已全自动部署完成，免费、稳定 |
| 腾讯 CloudBase | https://fat-loss-tool-prod-0504-da3dc1df-1428481707.tcloudbaseapp.com | ⏳ 待你扫码 | 你原来的域名，更新它需要你登录腾讯云一次（见第四节）|

> 个人主页（Codex 负责）项目卡里，建议优先填 **GitHub Pages 地址**，因为它已经是最新版且无需维护登录态。

---

## 三、版本关系（你说的"第一版 5/1"）

- **第一版（5/2 前后）**：纯静态网页 + PWA，部署在腾讯 CloudBase（就是你给我的那个域名）。
  - 原始工程在：`Documents/Codex/2026-05-02/files-mentioned-by-the-user-excel/`
  - 数据来自 B 站「好人松松」的健身 Excel 超级套表（25 年 9 月版）+ 视频转写。
- **最新版（6/1）**：用 React 重写，忠实复原同一套算法，结构更干净、可维护、可嵌入主页。
  - 工程在：`Documents/碳水蛋白质配额卡/`
- 旧版线上快照存档在本项目 `_live-snapshot/`，已和第一版逐文件比对一致。

---

## 四、以后想更新网站怎么办（两种）

### A. 更新 GitHub Pages（推荐，全自动）
改完代码后，在项目目录里执行：
```
git add -A && git commit -m "更新" && git push
```
推送后 GitHub 会自动构建并上线，约 1 分钟。无需任何账号密码。

### B. 更新你原来的 CloudBase 域名（需要你登录一次）
在项目目录里执行：
```
npm run deploy
```
第一次会弹出浏览器让你扫码登录腾讯云（用减脂工具所在的账号扫），之后自动构建并部署到线上域名。

---

## 五、关键文件索引

| 文件/目录 | 作用 |
|---|---|
| `src/lib/calculateQuota.ts` | 核心算法（本次未改动）|
| `src/data/` | 配额表、有氧、分餐等数据（未改动）|
| `dist/` | 构建出来的上线产物（自动生成）|
| `cloudbaserc.json` | CloudBase 部署配置 |
| `scripts/deploy.sh` | 一键部署 CloudBase 脚本（`npm run deploy`）|
| `.github/workflows/deploy-pages.yml` | GitHub Pages 自动部署 |
| `_live-snapshot/` | 旧版线上快照存档 |
| `iterations/v1-launch/` | 产品文档/迭代记录 |

---

## 六、还差一步需要你做的

- 想更新**原 CloudBase 域名**：跑一次 `npm run deploy` 并扫码（GitHub Pages 已自动上线，无需此步）。
- 告诉 Codex：个人主页项目卡用 GitHub 地址
  - Demo: `https://hou-152.github.io/carb-protein-quota-card/`
  - GitHub: `https://github.com/hou-152/carb-protein-quota-card`
